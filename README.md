# analyzer
A simple tool to capture packets and save as pcap and send over http to the remote

# How to build it
Run this command to build as binary file
```
go build -o analyzer
```
# WORK IN PROGRESS !!!
